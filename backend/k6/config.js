export const BASE_URL = __ENV.BASE_URL || 'http://localhost:3000'; // local
// export const BASE_URL = __ENV.BASE_URL || 'http://host.docker.internal:3000'; // docker

export const DEFAULT_HEADERS = {
  'Content-Type': 'application/json',
};
